# רשימת משימות – TODO

**נוצר:** לפי סיכום עם החבר והבדיקה בפרויקט.  
**עודכן:** 08.02.2026 – סימון הושלם (המלצות, הזמנות, כמות בדף מוצר), וסידור נשאר / לא רלוונטי.

---

## הושלם לאחרונה (לא ברשימת המשימות הפתוחות)

- **ייבוא CSV מוצרים:** ב־`ProductService.importProductsFromCsv` – אם מוצר עם אותו `id` קיים ב-DB מעדכנים אותו (כולל `imageUrl`); אחרת יוצרים מוצר חדש.
- **תמונות בהמלצות:** ב־`RecommendationController.getImageUrlForProduct` – עדיפות ל־`imageUrl` מהמוצר ב-DB; רק אם ריק – fallback ל-URL heurístico.
- **Seed מוצרים:** ב־`DataInitializer` – טעינה ראשית מ־`datasets/raw/products_10000.csv`, אם לא קיים – fallback ל־`seed/products_10000.csv`.
- **פרונט – Layout, Navbar, דפי Categories ו־On Sale** (כבמשימות המקור).
- **CORS, עגלה, Checkout, ארנק, Stripe Customer** (כבמשימות המקור).
- **הזמנות:** לאחר רכישה – `completeCheckoutForUser` מעדכן `purchaseHistory`; **עודכן 08.02:** הזמנות נטענות מהשרת בהתחברות (`getPurchaseHistory`), ו־`refreshOrders()` אחרי צ'קאאוט. תצוגת "Order history" בפרופיל עם מוצרים מהשרת (כולל מחיר ותמונה).
- **המלצות – איפוס אחרי רכישה:** איפוס קאש ML אחרי תשלום (גם בארנק); TTL 30s. מיון לפי **משקל רכישות** (לא צפיות) כדי שקטגוריות שרכשת (למשל fitness) יופיעו ראשונות.
- **המלצות – משתמש ללא אינטראקציות:** "המלצות" = אותם מוצרים כמו "פופולריים" (ניקוי לפי id + שם, מיון לפי views).
- **כמות בדף מוצר:** בורר כמות (1–99) בדף פרטי מוצר; "Add to Cart" ו-"Buy Now" מוסיפים את הכמות שנבחרה.
- **חיבור DB, Seed** (כבמשימות המקור).

---

## 1. דאטה ובדיקות

- [x] **למלא את הדאטה־בייס לאתר**  
  הושלם ואומת.

- [ ] **בדיקת תמונות**  
  לוודא שהתמונות באתר טוענות כראוי (כולל אם הבעיה רק בסביבה מסוימת).

- [ ] **תיקון תמונות מוצרים (להמשך)**  
  בוצע חלקי: שימוש ב־`imageUrl` מה-DB/CSV. **נותר:** אי־התאמות (תמונה גנרית, אותה תמונה למספר מוצרים) – מיפוי לפי קטגוריה או מקור ייחודי לכל מוצר.

---

## 2. שירות ה-ML (Flask) מול הפייפליין האמיתי

- [ ] **חיבור Flask למודלים האמיתיים**  
  `backend/ml_service/app.py` כרגע דמו (in-memory, פופולריות + קטגוריות לפי רכישות). לחבר ל־`recommendation_system_ml.py` או שירות שרץ את המודלים האמיתיים אם רוצים.

- [ ] **קטגוריזציית מוצרים – שימוש ב-XGBoost**  
  המודל ב־`src/phase1/product_categorization.py` רץ אופליין. להוסיף שימוש במודל (או endpoint) לקטגוריזציה בזמן ריצה אם רלוונטי.

- [ ] **קטגוריזציית משתמשים – API**  
  להחליט אם צריך API ל"רישום/עדכון משתמש + קטגוריזציה" ולחבר ל־`user_categorization.py` אם רלוונטי.

---

## 3. פרונט ו-Admin

- [ ] **יצירת משתמשים / טריגר קטגוריזציה ב-Admin**  
  אופציונלי: מסך ליצירת משתמשים ו/או כפתור להרצת קטגוריזציה.

- [ ] **אימות Admin בפרונט**  
  לוודא שכל קריאות ה-Admin API שולחות את ה-email של המחובר (header `X-User-Email`). בפרונט נשלח ב־`api.ts` ב־createProduct, updateProduct, deleteProduct, getProductsPaginated – לבדוק שזה מספיק לכל האנדפוינטים.

---

## 4. תיעוד ובריאות

- [ ] **עדכון דוקומנטציה**  
  לעדכן README, API docs, PROJECT_STATUS לפי השינויים (המלצות, הזמנות מהשרת, כמות בדף מוצר).

- [ ] **Health/דיאגנוסטיקה ל-ML**  
  ב-ML יש כבר `/health` ו־`/status`. אופציונלי: endpoint בבקאנד (למשל `/health/ml`) שבודק חיבור ל-ML.

- [ ] **תיעוד "מה רץ איפה"**  
  מסמך קצר: מה רץ ב-Flask (דמו), מתי קוראים ל־`/data/load`, איך אימון/קטגוריזציה רצים.

---

## 5. אוטומציה והרצה דינמית

- [ ] **הרצת Phase 1 (אימון) מה-backend**  
  להחליט אם להריץ אימון (user_categorization, product_categorization) אוטומטית/מתוזמן מה-backend.

- [ ] **הרצה דינמית של למידה**  
  להגדיר מנגנון להרצת למידה לפי דרישה או לוח זמנים (אופציונלי).

---

## 6. סנכרון ו-Order

- [x] **סנכרון דאטה Backend ↔ ML**  
  **מבוסס:** לפני קריאות להמלצות קוראים ל־`/data/load` (עם TTL 30s); אחרי רכישה מאפסים קאש. ה-ML מקבל products, users, interactions (עגלה, רכישות, צפיות) מהבקאנד.

- [ ] **Order / הזמנות בשרת (אופציונלי)**  
  כרגע: `purchaseHistory` ב-User + API `GET /api/users/{id}/purchase-history` לתצוגה. אופציונלי: מודל Order נפרד (מספר הזמנה, תאריך, פריטים) אם רוצים היסטוריית הזמנות מפורטת יותר.

---

## לא רלוונטי כרגע / להחליט בהמשך

- **רענון המלצות אחרי התנתקות:** התנהגות נכונה – אורח רואה פופולריים; אחרי התחברות שוב נטענות המלצות אישיות. לא באג.
- **קובץ backend/TODOLIST:** רשימה נפרדת (RabbitMQ, ElasticSearch, ML container וכו') – להחליט אם לממש או לאחד עם רשימה זו.

---

## הערות

- Register API ו-Admin API למוצרים **כבר קיימים**.
- דף Admin עם יצירה/עריכה/מחיקה/ייבוא CSV **כבר קיים**.
- **FILES_TO_CLEANUP.md** – קבצים לניקוי (גיבויים, `datasets/unused/`).
